# Adapters separate placement from compatibility

Target-agent adapters will guarantee only that a directory skill is placed in the agent's recognized project location. A skill's compatibility declaration remains advisory because structurally installable instructions can still be semantically agent-specific.

## Considered Options

- Treat successful placement as compatibility
- Separate adapter support from a skill's compatibility declaration
